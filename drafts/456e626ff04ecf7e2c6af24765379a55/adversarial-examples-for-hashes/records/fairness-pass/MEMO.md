# Heuristic hashes, second pass: fairness, currency, witness strength

Prepared 2026-09-19 for the author of "Adversarial examples for fast hash functions". Inputs: one reviewer report and one independent skeptic verdict per hash (27 rows), both run against fresh upstream clones on the Xeon 8375C. Where the two disagree, the verdict wins and is marked "verify" in fixes.json; items only the review established are marked "review".

Headline: 26 of 27 rows reproduce on the newest upstream source and no quoted claim is fabricated or misquoted in substance. The problems are (a) three rows (wyhash, rapidhash v1, rapidhash v3) scored under a key model that the page's own convention contradicts, (b) MuseAir, where the pair does not transfer to the current upstream algorithm and the page does not say so, (c) one row (XXH3-64) where a 15-second probe found a witness six bits stronger than the one on the page, and (d) a long tail of stale sentences, mislabelled versions and dangling provenance.

## 1. Per-hash table

Columns: tested version | latest upstream | core changed since tested? | pair on latest (reproduces / rate ± 95% CI / fails) | witness strength | claim quoted correctly? | action.

| id | tested | latest | core changed? | pair on latest | witness | claim OK? | action |
|---|---|---|---|---|---|---|---|
| city | CityHash64 v1.1.1 (SMHasher3 7ad8939d) | google/cityhash master f5dc541 (2022-07-19, no tags; v1.1.1 2013 is the last release) | No (NFC only) | Reproduces: every seed; 2^24/2^24 on WithSeed and WithSeeds, two independent runs; Peters' v1.0.3 strings verified not to collide on v1.1.1 (and vice versa) | Strong: 0 bits, metric floor, structural proof | Yes | Wording only: stale "no trial count" sentence, Peters 2024 / three authors, truncated data.json fields, dangling provenance, add v1.0.3 converse |
| farm | FarmHash NA v1.1 | google/farmhash master 9d99331e (2026-06-05); v1.1 (2015) only release | No (8 portability/NFC commits) | Reproduces: 2^26/2^26 on 30 pair×entry-point cells (na/uo/xo/te, both seed APIs) in two builds; DebugTweak only changes displayed value | Strong: 0 bits, floor | Yes | Wording only: stale sentence, truncated field, verify README "not run here" now run, DebugTweak note; do not write "only change is PR #25" |
| murmur | MurmurHash3_x64_128, SMHasher3 seed truncation to u32 | aappleby HEAD 07bb4de (2026-06-24), src unchanged since 2016; SMHasher3 3b619371 file unchanged since 2023 | No | Reproduces: all 2^32 seeds, two independent enumerations, controls 0 | Strong: 1.59 cap = key-free floor (L=2 excluded; min k2 difference ≈2^56.6) | Yes (wiki + issue #73) | Wording only: caption, output-order note, truncated field, L=2 note, provenance |
| mx3 | v3.0.0 = 48924ee7 | Same commit is master HEAD | No | Reproduces: 2^30/2^30 for 1/8, 7/8 and 16/16 pairs, two programs; 0/2^20 on v1/v2 | Strong: 0 bits | Yes | Wording only: caption, explain 2^30−1 trial count, 16-byte family now re-checked, cosmetic claim text, no private paths |
| fasthash-64 | 08a25db2 (only code commit) | ae4eca64 (README-only); open PR #4 keeps constants | No | Reproduces: 2^30/2^30 × 3 pairs × 2 widths, two independent drivers linked to upstream fasthash.c | Strong: 0 bits | Yes | Wording only: "claimant-only" caveat for pair C stale, jargon, residue, rurban attribution, 1≤a<b≤8 |
| fasthash-32 | same | same | No | Reproduces: exhaustive 2^32 on upstream uint32 API (both pairs), 2^30 on 64-bit API | Strong: 0 bits | Yes | Wording only: label 01274ad8 (SMHasher3 model) vs 1188bc14 (upstream API), PR #4 note, records line 73-74, interpretation |
| muse | MuseAir v0.3 (SMHasher3) | v2 / crate 0.6.0 (2026-07-13); v0.4, v1 in between | YES: v2 XORs seed into tail multiplicands | Reproduces on v0.3 and v0.4 (2^26, 2^24, all four APIs); FAILS on v1 and v2: 0/2^24 on all four APIs (sample, not bound) | Strong for v0.3: 1.59 cap | Yes for v0.3 (withdrawn); current README position not shown in table | TEXT: say v0.4 affected and v2 not; quote current claim; MuseAir #4 in Disclosure; "C re-implementation" wording |
| komi | 5.34 witness; SMHasher3 "v5.27" timings (no such tag) | 5.34 = HEAD 50a15150 (2026-08-04) | No | Reproduces: 0.910583 (2^32, deterministic default stream) and 0.910559 (independent 2^28); 5.27 vs 5.34 headers agree on 200k inputs | Strong: 3.135 cap within 0.14 bits of the L=8 floor | Yes | Wording: "supplied" jargon, 128-B null resolutions mis-stated, caption, version_note, cite issue #23 as disclosure |
| t1ha | t1ha2_atonce, tag v2.1.4 | v2.1.4 newest tag; 00eb779 docs-only; GitHub archived, primary is GitFlic | No (ARM load guards only) | Reproduces: conditional 2^-4.000 at 2^27; uniform 44 and 27 per 2^35 (pooled 111 vs 112 expected) all in class | Adequate: 31-bit cap; digit-route re-search might shave 1-2 bits | Verbatim, but claim_category "keyed collision / DoS claim" overstates what upstream claims | DATA: claim_category → "statistical quality only"; complete truncated field; README snapshot wording; pooled figures |
| a5 | a5hash v5.21 64-bit (b0ba799) | tag 5.25 ebe4755 (2025-12-12); main 10492db README-only (2025-12-19); SMHasher3 still v5.21 | No (macros/comments) | Reproduces exactly: 61,865,984 = 118×2^19 class seeds all collide on v5.25 and v5.21 headers; control 0/2^24 | Adequate: 47.81 cap; unexplored lengths named in records | Verbatim, but mislabelled "v5.25 wording": sentence exists only on main after the 5.25 tag | DATA/TEXT: relabel in both a5 rows; remove stale "not vector-checked" (5 files); add 2^-62/2^-59 contrast |
| a5wide | a5hash128 v5.21 | same | No (a5hash128 body identical) | Reproduces: 2^26/2^26 + 8 structured seeds; control 0/2^26; also 2^20 on tag 5.25 and b0ba799 | Strong: 1.59, floor for mechanism | Same label issue | Same relabel; do not write "identical sentence at tag 5.25" |
| wyhash | final v4.3 (= SMHasher3 "v4.2" body) | HEAD e4764a0b (2026-03-23); wyhash() body unchanged since 2023-10-06 | No | Reproduces: 9 recorded seeds; rate 2^-26.42, 766/2^36 pooled, cap 28.42 [28.32, 28.52] | WEAK under the row's own key model: 16-B key-free pair (a = secret[1] annihilation) collides for every seed, 1.0 bit; author acknowledged in issue #15 (2019) | Yes | DECISION: rescore key-free (page convention, a5wide/MUM precedent) or restate model as random-secret; fill empty speed cells; fix claims.json provenance |
| rapid1 | v1.0 tag 5889784 (tag 2024-07-07, release 2024-08-06) | rapidhash_v3 bc4b4ba; master 1ae7842f (2026-08-19) | Yes (v1 frozen tag; v3 separate row) | Reproduces on the tag: 12/12 recorded seeds; 89/2^33 → 28.5 [28.2, 28.8] | WEAK under model: 24-B every-seed pair (1.58 bits) in results_determ.md and issues #10/#25 | Verbatim, but issue #10 (what was reported) and #25 uncited | DECISION + cite N-R-K #10 and hoxxep #25; replace stale "supplied source is v3" text; caption |
| rapid3 | v3 = upstream 34f4515 (SMHasher3 29e3090) | master 1ae7842f (#pragma once only) | No | Reproduces: 98/2^33 → 28.4 [28.1, 28.7]; every-seed annihilation pairs (16 B w0=secret[1]) verified on master for standard/micro/nano | WEAK under model: 1-bit key-free pair; a5hash-128 row already scores this mechanism as key-free | Yes | DECISION consistent with wyhash/rapid1; P3 pattern; protected-mode measured (annihilation gone, fold pairs 4/2^28 remain); seed-0 guidance from #10 |
| foldhash-fast | 0.2.0 = 8f878c6 | 0.2.0 newest (crates.io); master 77d8e3d cfg-only | No | Reproduces: 2761/2^38, 699/2^36 (verifier); 642 & 674/2^36 via crate public API | Strong/adequate: 26.57 at the differential's natural rate | Mostly: two README sentences joined without the intervening "minimally DoS-resistant" sentence | TEXT: disclosure stale (issue #50 filed and answered), ellipsis/third sentence, broken sources/foldhash-0.2.0 link, version currency |
| foldhash-quality | same | same | No | Reproduces: 732/2^36 (verifier, 8 threads); crate scored model 171/2^34, from_u64 83/2^33 | Same | Same | Same; do not claim "1..7-byte complements" (records cover 2, 4, 7 only) |
| mum | "v3" unpinned; SMHasher3 port pre-fix, paper harness post-fix | 595c091 (2026-06-02); fix 52c7d6c 2025-11-28 | Yes, but not on the 8-byte path | Reproduces every seed on default, MUM_V3, V1, V2, both unrolls (2^20 each, 2 programs); MUM_QUALITY 0/2^20 but has its own every-seed 8-B pair | Strong: 0 bits | Yes | TEXT: "pre-fix v3, not the fixed default" is wrong (fixed default still collides); harness mislabelled; pin code_url; speed sentence source/commit |
| mir | arithmetic since 2019-04-09; v1.0.0 | master a8ab7c31 (2026-06-19) | No | Reproduces: 2^30/2^30 both pairs both variants; direct upstream port PASS | Strong: 0 bits | Yes | Wording: verify README template leftovers; version string; keep "reproduced" and "≤ 0 bits" (page-wide) |
| xxh3-64 | v0.8.3 | v0.8.3 newest; dev 6cc7b4b (2026-09-18) core identical (2.4M-case differential test) | No (comments; ARM pragma guard) | Reproduces: 483/535 replayed, 508 and 533 fresh per 2^30 → 23.01 | ADEQUATE but beatable: (k0,~k1)‖0^16 vs complement collides 2^-15.17 (three 2^30 runs, cap 17.17) | Spec sentence verbatim and current; xxhash.h DISCLAIMER replaced 17 Sep (PR #1146), refined 18 Sep (PR #1150); issue #1127 closed | TEXT: maintainer update stale; adopt the structured pair after running the verify pipeline; README/program; claims.json |
| xxh3-128 | v0.8.3 | same | No (functionally) | Reproduces; 165/2^34 → 28.6 [28.4, 28.9]; the recorded 5/2^30 (29.7) was a low draw | Adequate: cap; random-base family flat at 2^-26.6 | Yes; DISCLAIMER chunk superseded on dev; passed 214 vs 213 at the linked commit | DATA: rescore 28.6; fix smhasher3 url/count; maintainer update; local path in README |
| highway | f8381f3 (2024-04-18) | same = HEAD, 0 commits since, frozen | No | Reproduces exactly (E_2 rate within 2σ of 235·239/2^40 in every sample; explicit key collides at 64/128/256) | Strong: 59.81 exact; 59.28 certified 72-B cap on record | Mostly: "at most 1 in 2^64" is §6.2, not §6.3 | TEXT: §6.2; 3,458/2^36 has no record anywhere (only a independent verifier draft); regenerate three run files; 59.28 hover |
| spooky | SpookyHash V2 (2012) | same; mirror c3bbfb6 (2026-03-29) rust-only | No | Reproduces: 0.50001 (2^30, two harnesses on reference code); independent seed words 0.50002 (2^28) | Strong: 6.13 cap at the trail's ceiling | Yes | Wording: three unlabelled figures for the 286-B pair; log2 computed from "0.5001"; stale "unreproduced"; pseudo-code header |
| pengyhash | v0.3 = upstream 9b70a18e via SMHasher3 | same, static since 2022-11-30 | No | Reproduces every seed: 2^30 (verifier), 2^24 × 4 compiler builds of raw upstream source | Strong: 2 bits, floor for the bulk-loop mechanism | Verbatim, but it is the author's issue #1 reply, not README; table links README | TEXT: link the issue comment; "the task" jargon; template leftovers; key_model residue |
| nmhash32 | v2, e022156 | same = HEAD; SMHasher3 file unchanged | No | Reproduces exactly: 1078944392/2^32 in AVX-512 and scalar builds of upstream header | Strong: 4.993, ε=1/4 is the trail ceiling | Yes | TEXT: Xeon speed cells wrongly carry the ‡ verification-failure tag (only M2 fails); README "AVX2"; template leftovers |
| nmhash32x | v2 | same | No | Reproduces: all 2^32 seeds × 3 pairs, SSE2 and AVX-512 builds | Strong: 2 bits | Yes | TEXT: same ‡ tag; claim context is a timing note; 32-bit seed printed as 16 hex digits; M2 number times a mis-built port |
| gx | 3.5.0 / SMHasher3 port of 55bde47 | 55bde47 = HEAD; crate 3.5.0 newest | No | Reproduces: C verifier 2^26 (AES-NI) + 2^24 (portable); Rust crate at HEAD 2^22 both widths, control 0 | Strong: 1.0 bit, floor | Yes | TEXT: trial-count sentence cites numbers not in shipped logs; caption; unbalanced parenthesis (also in source record) |
| ahash | 0.8.12 AES path | 0.8.12 newest; master a9d649d (2025-11-20) core identical | No | Reproduces natively in Rust (v0.8.12 and master): 39848/2^35 → 22.52 [22.51, 22.54]; recorded 2616/2^31 was 2.5σ high | Adequate: 3 active S-boxes is the floor; ≤1 bit from a trail search | Yes | DATA/TEXT: score 22.52; "no native Rust run" stale; ARM gate chronology; link #292; truncated fields |

## 2. Where the page must change, by severity

### Tier 1: pair fails on the latest version, or a claim/attribution is wrong

1. **MuseAir (muse).** The pairs do not collide on the current upstream algorithm (v2, crate 0.6.0, 0/2^24 on all four APIs) but do on v0.4; the page says only "this witness concerns v0.3". Add: "The same pairs collide for every seed on upstream v0.4 (crate 0.4.0, 24 July 2025). Upstream moved to algorithm v2 (crate 0.6.0, 13 July 2026), whose tail products XOR in the seed; against v2 the same pairs gave 0 collisions in 2^24 seeds on all four APIs (a sample, not a bound). v1 (crate 0.5.1) is likewise unaffected by these pairs (0/2^24) though it has Peters' seed-free tail. This witness concerns the v0.3 code SMHasher3 still measures." Quote the current README (line 82, in full) alongside the withdrawn v0.3 sentence and the positive current claim; add MuseAir issue #4 (2026-09-18, open) to the Disclosure paragraph; fix official_claim.context ("dropped in v0.4", not "withdrawn by v2"); "the author's C re-implementation, validated against the four SMHasher3 verification values, found 2^26/2^26" (not "against SMHasher3 3b619371").
2. **wyhash, rapidhash v1, rapidhash v3 (wyhash, rapid1, rapid3): key-model inconsistency.** All three rows declare "Uniform 64-bit API seed; fixed default public secret/constants" and define key-free as "collides for every API seed, with the stated public constants". Under that model, verified every-seed pairs exist: wyhash 16 B `934bb88b14151617c9ac2e961c1d1e1f` / `934bb88b14151617c9ac2e96acadaeaf` (H = 0x951ec28da2be0730 for every seed, 1.0 bit, acknowledged by wangyi-fudan in issue #15, 2019); rapidhash v1 24 B `0000000000000000c9ac2e96934bb88b0000000000000000` / `01…` (0xf3d82622ec4bad54, 1.58 bits; issues #10, #25); rapidhash v3 16 B `c9ac2e96934bb88b` ‖ any second word (0xa50a4a2a22949120, 1 bit). The a5hash-128 row already scores a shipped-constant zero-operand pair as key-free (P3), and MUM v3 is key-free under identical model wording, so the table is internally inconsistent and ~27 bits too generous to wyhash/rapidhash. Author decision, applied to all three at once: (A) rescore key-free with the annihilation pairs, keeping the fold differentials as supporting measurements (the salted-secret cap); or (B) restate key_model as the paper's random-secret model, disclose the key-free pairs in notes and text, and list rapidhash/wyhash under P3 in "Recurring patterns". Either way: cite issue #10 ("Seed independent collision", N-R-K 2024-11-23, same w[len−16] = secret[1] mechanism) and #25 (hoxxep 2025-05-24), and Nicoshev's 2026-01-11 comment that random seeds are not recommended and most users use seed 0; credit wyhash #15 as prior disclosure; do not write "the metric's full-secret model" (the phrase is the paper's, not the post's).
3. **XXH3-64 and XXH3-128: maintainer update stale, wrong PR attributed in draft fixes.** "said the mixing-function comment will be updated" is stale; the DISCLAIMER above XXH3_mix16B was replaced by PR #1146 (merged 2026-09-17 22:38 UTC, commit 4c19ec2) and the ~2^-21 fixed-pair sentence plus the v0.8.4 CHANGELOG credit were added by PR #1150 (2026-09-18 19:37 UTC, commit 6cc7b4b); issue #1127 is closed; follow-up comment "updated documentation, mentioning this study and its results." Do not attribute the rewrite to #1150. Prefix verbatim_record "(v0.8.3 comment; replaced on dev 2026-09-17)". xxh3-128: passed 214/250 is the count at 7ad8939d; the linked 5035a923 table says 213; align url or count.
4. **a5hash-64 and a5hash-128: "v5.25 wording" is a wrong version label.** The quoted "practically resistant to blinding multiplication attacks" sentence exists only in the main README at 10492db (2025-12-19, after tag 5.25). The 5.21 and 5.25 READMEs say "practically secure against the "blinding multiplication" attack when the UseSeed is kept secret and hash outputs are not exposed" (quote both conditions). Fix context/hover/table cells in both rows; permalink the claim URL. Remove "v5.25 not vector-checked" from data.json notes/qualification AND records/speeds_existing.json:14508, records/speeds.json:21971, figure/data.json:800 and :1782. Add the README's own 2^-62/2^-59 BM estimate next to the counted 2^-38.12 class (fair, version-stable).
5. **t1ha: claim_category overstates.** Upstream claims only "Great quality of hashing" and disclaims cryptography; set claim_category to the existing "statistical quality only" (used by 17 rows), not a new label.
6. **pengyhash: claim attributed to the README.** "Version 0.3 passes these tests." is the author's reply on issue #1 (2022-11-30, comment 1331634982); the README does not contain it. Link the comment permalink / label "(author, issue #1)".
7. **HighwayHash: wrong section, unsupported figure.** "at most 1 in 2^64" is §6.2 "Differential Attack" (p. 13), not §6.3 (three places). The sentence "Two separate large class-key runs agree with the count: 3,458/2^36 … and 1,723/2^35" has no log anywhere (the 2^36 figure appears only in a independent verifier issue draft that independent verifier's own REVIEW.md flagged; 1,723/2^35 is an internal reviewer file); replace with on-record runs (11/2^28, 47/2^30 shipped; 51/2^30, 98/2^31 in records/highwayhash-count) and say "consistent with the count (every 95% interval contains it)", not "within 1σ".
8. **foldhash (both rows): verbatim claim is not verbatim, disclosure stale.** The two blockquotes drop README lines 267-269 ("It is these two properties that we refer to when we claim foldhash is "minimally DoS-resistant": it does the bare minimum to defeat very simple attacks.") without an ellipsis, in index.html and data.json for both rows; restore the sentence or add "[…]". "Disclosure: not yet reported to the maintainer" (index.html line 4583, data.json foldhash_addition.disclosure, both verify READMEs) is stale: orlp/foldhash#50 opened 2026-09-18 15:58 UTC, orlp replied 19:14 UTC ("not too surprised given the ad-hoc nature of folded multiply"; cross-type collisions out of scope); issue open. The "Local source" link sources/foldhash-0.2.0/lib.rs and both verify READMEs point at a directory that does not exist.
9. **MUM: the caveat is backwards.** "This is pre-fix MUM v3, not the later collision-prevention default" implies the fixed default escapes the pair; it does not (every seed on default/MUM_V3/V1/V2 at 595c091; the fix only guards the paired-word bulk loop for inputs > 8×unroll bytes). Also "measured through the paper harness's re-implementation" of pre-fix v3 is wrong: hashes.h already carries the post-fix `_mum_xor` guard. Pin code_url to 595c091; the speed sentence should name SMHasher3's own results table at 7ad8939d (the linked 5035a923 table shows 33.44/5.94, not 27.39/5.97).

### Tier 2: version currency and measurement updates

10. **XXH3-64 witness.** Adopt the structured pair M = `b8fe6c3923a44bbe83fe7ed308de52e3` ‖ 0^16, M′ = `470193c6dc5bb4417c01812cf721ad1c` ‖ 0^16: 29261, 28996 (dev) and 29147 (v0.8.3) per 2^30, pooled cap 17.17 bits [17.16, 17.18]; collides deterministically at seed 0 (multiplication by zero, already documented). Keep base-1143 as historical_pair_base1143; state it came from a 15-second probe of structured bases (search result, not bound). Requires: new startup witness in xxh3_64_32B_check.c, CHECK_32B.md and README, and a rewrite of the "no practical flooding attack at ~2^-21" sentences for 2^-15.
11. **XXH3-128 score.** Replace 5/2^30 → 29.7 [28.5, 31.3] with 165/2^34 → 28.6 [28.4, 28.9] (or 86/2^33 → 28.57 [28.27, 28.90]); say the earlier count was a low draw whose interval contains the new estimate (29.7 lies outside the new interval; do not write "inside this interval's neighbourhood").
12. **aHash score and native run.** Score 22.52 (19964/2^34 or pooled 39848/2^35 = 2^-19.718, ±0.014); "No native Rust run is supplied" is stale (two independent Rust programs against v0.8.12 and master); ARM gate: master dropped nightly-arm-aes in PR #268 (2025-05-06) BEFORE the 0.8.12 release merge, which did not carry it, so the crates.io release still requires it; link tkaitchuck/aHash#292 (2026-09-18, open); "second model family" → "independent reimplementation".
13. **Version-currency clauses to add** (all verified unchanged): city (master f5dc541 no functional change since 2013), farm (9d99331e, NFC since v1.1), murmur (file unchanged through 3b619371, last 2023-02-12), mx3 (tag = master HEAD), fasthash (ae4eca64 README-only; PR #4 open), komi (5.34 = HEAD; SMHasher3 "5.27" has no tag), t1ha (v2.1.4 through 00eb779; GitFlic primary), a5/a5wide (5.25 identical core; SMHasher3 ships 5.21), wyhash (2ac9a50 renamed the guard; body since 2023-10-06), foldhash (0.2.0 newest; master cfg-only), mum (595c091), mir (a8ab7c31), xxh3 (dev core identical), highway (HEAD = tested), spooky (V2 2012; mirror identical), pengyhash (static since 2022), nmhash (HEAD = tested), gx (HEAD = ported commit), ahash (master core identical).
14. **"tested at upstream commit 7ad8939d"** appears in all 23 SMHasher3-registered Code lines and always means the SMHasher3 commit (hyperlinked). Do not reword one row; either change the template site-wide to "tested at SMHasher3 commit 7ad8939d" or leave it and add the project's own upstream commit as a separate clause (fasthash, komi, t1ha, a5, mum, xxh3, gx, ahash, nmhash all benefit). Same for "≤ 0 bits" (site-wide for key-free rows) and the "/* Both 64-bit hashes: … */" pair-comment template (9 rows).
15. **Dangling provenance.** data.json points at records/claims.json (19 refs), records/review-inputs/pairs_heur.json, records/review-inputs/claims.json, records/review-inputs/deep_panel.json, records/review-inputs/heur_panel.json, records/review-inputs/review_reports/… (163 "data/" refs) and none exist in the published directory. Either ship them under records/ or repoint to files that exist (verify/…, records/paper_rows.json#/n, records/verify-check.log). Never cite private Xeon paths (experiment/…) in data.json, README or the page; copy logs into records/ or verify/ and reference those.
16. **nmhash32 / nmhash32x speed cells.** The Xeon (avx512) cells carry "pre-existing implementation verification-code failure" and ‡, but Xeon verification PASSES (0x12A30553 / 0xA8580227); only the M2 scalar SMHasher3 binary fails (0x4B575DFB / 0x9CF42B2D) and therefore times a different function. Edit index.html (no generator exists); reword the ‡ footnote to say M2 only and not assert where the defect lies.
17. **komi 128-byte null.** The 816-cell scan resolves ~2^-20 per cell (scan.c's own print), not 2^-22; only the independent verifier 0/2^24 run gives 2^-22.4 for this pair. Cite each at its own resolution.
18. **spooky supporting pair.** Three different figures (8,389,753/2^24 = 0.500068 from the original search, recorded in records/speeds*.json; 8,387,160/2^24 and 33,552,409/2^26 in shipped logs; log2_contribution −0.99971 computed from the rounded "0.5001"). Recompute from one measured count (prefer the verifier's 536,879,630/1,073,741,823 or run_2^26.log) and label it; drop "independent-seed-word measurements remain unreproduced" (shipped run_2^24_mode1.log and two 2^28 runs reproduce it).

### Tier 3: wording, jargon, truncation, template leftovers

19. Internal-pipeline jargon in the public text: "supplied local records", "supplied checked record", "the task's original v0.2 label", "The task labelled the hash", "claimant only", "original implementation only", "supplied komihash/README.md" (city, farm, fasthash, komi, pengyhash, mir, nmhash). Replace with plain statements of what was checked.
20. Truncated data.json strings: city pair.example_seed_raw / implementation_validation; farm implementation_validation ("…Orson Peters' p"); murmur implementation_validation ("(verify.c,"); t1ha implementation_validation ("(len 0."); ahash example_seed_raw / implementation_validation. Editing residue: fasthash-64 version_record "-- corrected", key_model "-- corrected from 40"; pengyhash key_model "corrected from '<=32'"; records/remainder_rows.json rows/5 "lines 70-72" (fold is 73-74); a5 official_claim.context duplicated tail; gx other_pair.example_seed_raw missing ")" (also in pairs_heur.json source).
21. Verify-README template leftovers (NMHASH 16-bit products, fasthash32 seed interfaces, multi-project licence line, scratch paths "experiment/…", "source/…") in verify/mir, verify/pengyhash, verify/nmhash32, verify/nmhash32x, verify/xxh3-64, verify/xxh3-128; verify/cityhash-64 line 14 "(Aumasson-Bernstein 2012, Peters 2023)" → "(Aumasson, Bernstein and Bosslet 2012; Peters 2024)"; verify/highwayhash run_2p24/2p26/2p24_sm3 regenerated with the MANIFEST program (phrasing lines differ); verify/t1ha2-64 README "frozen copy of upstream t1ha v2.1 … 2021-12-09 snapshot" → core identical to v2.1.4 and 00eb779 incl. the 2022 ARM fix.
22. Stale caveats now superseded by reruns: city "no sampling trial count" (records/verify-check.log line 2 holds 2^20; README 2^24); farm same (line 3 "1048576 x6"); fasthash pair C "claimant-only"; mx3 16-byte family "original implementation only" (add it to mx3_verify.c or say "separate driver"); komi "New" now checked against the issue tracker (no prior lane-tie report; disclosed as avaneev/komihash#23); murmur output-order note (verify prints 46328447a2022b04964f2f614c774125).
23. Seed formatting: nmhash32x example_seed_raw "0x0000000000000000" and nmhash32 "0x00000000b54cda26" for 32-bit seeds; use 8 hex digits for both or neither. fasthash-32 examples: label 01274ad8 (SMHasher3, 64-bit seed) vs 1188bc14 (upstream uint32 API, seed truncated to 0x89abcdef).

## 3. Bounded searches worth running

| id | search | cost | expected gain | status |
|---|---|---|---|---|
| xxh3-64 | Neighbourhood search around (k0, ~k1): a few thousand structured bases at 2^24 seeds, then 2^30 on the best | < 1 CPU-hour | Already 6 bits (23.0 → 17.2) from a 15-s probe; further gain unknown, plausibly ≤ 2 bits | Run the verify pipeline on the found pair first (three 2^30 runs exist); adopt, then optionally extend |
| ahash | Small trail-search program: enumerate the two block-1 byte differences and inverse-S-box output choices so the third transition has DDT 4/256 instead of 2/256 while keeping the additive-lane cancellation | program to write; minutes to run | ≤ 1 bit (rate ~2^-18.7, cap ~21.5); shortening below 56 B unlikely (block overlap) | Not run; optional |
| a5 (64-bit) | Per-length 2^32 histogram of the residual product at the record's unexplored points: length 4567 (33-bit residual, L=571), heavy Seed2_init values, lengths ≤ 16 (57-bit residual) | minutes per length on 8 cores, 1-8 GB | fraction of a bit to ~1 bit below 47.81 | Not run; the row is a cap and says so |
| t1ha | Signed-digit route DP over lower-weight 128-bit multiples of P2 (costs 26/27/31/39 already found) | < 1 CPU-hour | 1-2 bits at most; tier unchanged | Not run |
| xxh3-128 | Structured sum-preserving differentials (w0+d, w1−d) beyond the Pair-F complement family | not obviously cheap | unknown; random-base family is flat at 2^-26.6 (2048 bases) | Not run |
| wyhash / rapid1 / rapid3 | none needed: exact algebraic every-seed pairs already verified (decision item 2) | 0 | 27 bits if the model is read literally | Decision, not search |
| mum (MUM_QUALITY) | done: 138-s Brent rho gives every-seed 8-B pair 4f14c5312ab68a6e / 9bff572f76b22816 | done | coverage only (default build already at 0 bits) | Mention in notes |
| komi | none: 8..64-B site scan (5333 cells × 2^26) null; cap within 0.14 bits of the L=8 floor | | | Skip |
| foldhash, highway, spooky, nmhash32, muse, all 0-bit rows | none: at or within noise of the mechanism's floor; muse v2 is a different algorithm | | | Skip |

## 4. Where the post is currently unfair to a project (plain statements)

- **MuseAir:** the page tests v0.3 and says upstream "moved to v2" but never says the pairs do not transfer to v2 (0/2^24 on all APIs) or that the maintainer's current README makes a different, narrower claim ("relies solely on an initial seed … unsuitable for direct use on servers"). A reader may think current MuseAir is broken by this pair; it is not (v1/v2 have their own issues per Peters, not these pairs).
- **wyhash and rapidhash (both versions):** the reverse problem. Under the page's stated key model the table shows them ~27 bits stronger than MUM v3 and a5hash-128, whose identical shipped-constant weaknesses are scored key-free. Whichever model the author picks, the current table is inconsistent across projects. Additionally, the rapidhash v1 row quotes Nicoshev's reply to issue #10 without saying what #10 reported, and cites neither #10 (N-R-K) nor #25 (hoxxep); the wyhash row does not credit wangyi-fudan's 2019 acknowledgment (#15) of the a = p1 annihilation. Both violate the cite-prior-work rule.
- **xxHash:** the page still says the maintainer "will" update the comment; he did so within hours on 17 September and credited the study in the v0.8.4 changelog on 18 September. Not crediting that is unfair to Cyan4973. Also the XXH3-128 cap 29.7 overstates the hash's strength by ~1 bit versus the better-measured 28.6.
- **a5hash:** attributing the "practically resistant" sentence to "v5.25" is a wrong version label (it is the post-5.25 main README); the tagged READMEs' wording, with its two explicit conditions, should be quoted alongside.
- **foldhash:** joining two non-adjacent README sentences without an ellipsis drops the sentence that frames the claim as "minimally DoS-resistant … the bare minimum", i.e. understates how modest orlp's claim is; and the maintainer's prompt reply to #50 is not recorded.
- **t1ha:** claim_category "keyed collision / DoS claim" attributes a claim upstream never made.
- **pengyhash:** the claim cell links the README for a sentence that is an issue reply.
- **NMHASH / NMHASHX:** the Xeon speed cells are tagged with a verification failure that only the M2 build has; and the M2 numbers time a mis-built port, which the footnote does not say.
- **MUM:** the page understates the result against the project (the fixed default still collides) while mislabelling the harness as pre-fix; both directions inaccurate.
- **HighwayHash:** cites §6.3 for a §6.2 sentence and a 3,458/2^36 run that has no record.
- **komihash:** the page's "New" claim is now backed by an issue-tracker check and the disclosure (#23); fine, but the 128-byte "no collisions at the tested resolution" states no resolution.

Everything else (city, farm, murmur, mx3, fasthash, mir, gx, spooky, nmhash, highway score, komi score) is accurate to the project; the remaining items are staleness and bookkeeping.

## 5. Cross-cutting rules for the edit pass

- Change page-wide templates page-wide ("tested at upstream commit", "≤ 0 bits", pair-comment), never one row.
- Apply the wyhash/rapid1/rapid3 decision in one commit, with the a5hash-128 precedent named in the text.
- Every number on the page must trace to a shipped file (verify/…, records/…); copy Xeon logs into records/ before citing them; never cite <xeon-work> paths.
- When a new sample replaces a score, keep the original count as a supporting measurement with provenance; say why the sample changed (thread count changes the deterministic stream; larger sample), and never call the old count "lucky".
- Regenerate verify/MANIFEST.json sha256 entries for every edited verify file.
