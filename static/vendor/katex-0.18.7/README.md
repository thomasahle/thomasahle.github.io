KaTeX 0.18.7, from the official npm package:
https://registry.npmjs.org/katex/0.18.7

The package tarball was checked against its published SHA-512 integrity value.
`katex.min.js`, `katex.min.css`, and `fonts/` are unmodified distribution files.
See LICENSE for the MIT license and https://katex.org/docs/browser.html for usage.

To update, copy the same distribution files and fonts from a pinned release and
update the versioned asset paths in the article. Keep fonts beside the stylesheet.
