# Witness page pass — 2026-09-19 UTC

Updated the live article in place. Backups are `index.html.bak-20260919T002142Z` and `data.json.bak-20260919T002142Z`.

## Scientific changes

All 34 patch actions across five row IDs were applied. The aHash placeholder was completed from the independently reproduced upstream log, with public API arguments distinguished from internal key words.

| Row | Previous cap | Selected cap | Witness |
|---|---:|---:|---|
| XXH3-64 0.8.3 | 23.01 | 12.47 [12.473, 12.476] | 32 B, L=4; 2,264,081/(3·2^30), block-1 complement |
| t1ha2_atonce-64 | 31.0 | 29.19 | F60, 13/16 B, L=2; 2^-24 × 58,715,203/2^30 |
| aHash 0.8.12 AES | 22.52 | 22.18 [22.16, 22.21] | A_2, 56 B, L=7; 12,627/2^33 |
| MuseAir v2, crate 0.6.0 | new row | 19.45 [19.43, 19.46] | 32 B, L=4; 36,060/(1.5·2^32) |

MuseAir v0.3 retains 1.59 bits and cross-references v2. The a5hash scientific row is byte-for-byte unchanged as a JSON value: its 47.81-bit witness survived all 55 exhaustive (length, value) points. Historical XXH3 pairs remain explicitly labelled as controls. Seed 0 is not a witness for either selected XXH3 or MuseAir v2 pair.

The timing lane completed during this pass. MuseAir v2 is chart eligible on both hosts using its own validated registration: M2 Pro 10.42 B/cycle and 22.32 small-key cycles/hash; Xeon 7.53 B/cycle and 29.27 small-key cycles/hash. The timing protocol is M2 median-of-three and Xeon higher-bulk/lower-small-of-two. No v0.3 timing was reused; no >15% run/control flags were reported. [Timing report](records/witness-searches/muse-v2-timing/REPORT.md).

## Page and packages

Updated both tables, the three changed appendix entries, the new MuseAir v2 appendix, explicit pairs and keys, derivations, claim excerpts, profile results and expanded notes. The disclosure paragraph states that the existing MuseAir #4, aHash #292 and xxHash #1127 threads quote the old figures and follow-ups are pending; the t1ha email draft also needs updating.

Refreshed `verify/xxh3-64`, `verify/t1ha2-64`, `verify/rust-ahash`, and added `verify/museair-v2`. Historical material remains labelled separately. Corrected the stale 56-byte `confirm_pairs.txt` line 2 to `B_0_STALE56B`; the true 64-byte B_0 and statistically tied A_1 are included as alternates. Native Rust crates and lockfiles are supplied. Rebuilt the three browser WASM modules with the new messages, keys and class metadata; the aHash input label now explicitly says internal RandomState words.

Records are linked under `records/witness-searches/`. Nested independent verifier records were also copied to the canonical relative paths in the patch input; the missing aHash search README was supplied from the memo and verification results.

## Validation

Each package ran on the Xeon with `taskset -c 24-31 nice -n 10`, with observed counts in its README and raw log:

- XXH3: 725/2^20 at both 32 and 128 bytes; zero 24-byte hits, identical 16-byte prefixes excluded; full-output/fold and 32/128 event checks pass.
- t1ha F60: 916,680/2^24 class samples and zero uniform hits; explicit witness and upstream KAT/SMHasher3 checks pass.
- aHash A_2: C 27/2^24 independent-word hits; native crate 89/2^26; explicit output `21a01e6ddf40c84a` reproduced. A_1, B_0 and historical controls also checked.
- MuseAir v2: C counts 84/84/81/81 in 2^24; native crate 388/388/387/387 in 2^26 across the four APIs; explicit witness and seed-0 noncollision reproduced.

These are package validation runs; they do not replace the larger confirmation estimates in the article. The deterministic `make check` subset passes for all four packages. [Xeon check log](records/witness-searches/page-pass/make-check-xeon.txt).

Ran `python3 blog/adversarial-examples-for-hashes/figure/build.py` from the website root: all 24 SVGs, 24 PNGs, figure data and feature assets regenerated. Only the mobile/compact log-view ChainHash v3 annotation was repositioned to clear the new point; scientific coordinates remain unchanged. The existing witness-context paragraph is now regenerated by the builder from the scientific record. Chart controls, inspector, keyboard help, fallback, caption, notes and the existing renderer remain intact.

Playwright passed at **375, 768, 1200 and 1620 px**: no horizontal document overflow, no page exceptions or local asset errors; both hosts, animation, proved/claimed/measured profiles with expanded notes, keyboard selection and Escape, hover, TOC targets, the three updated WASM demos, reduced motion, and the static image with JavaScript disabled. Screenshots were inspected at phone and desktop widths. [QA report](records/witness-searches/page-pass/visualqa.json).

The selected data, table values, appendix messages/outputs, figure source hashes and package manifests agree. No new broken relative article links were introduced. Regenerated all verification SHA-256 entries and `verify.tar.gz`; every archived file matches the manifest.
